
server:
  port: 8081

spring:
  cloud:
    config:
      server:
        git:
          url: https://github.com/VargasSantiago/Microservicios-SpringBoot-SpringCloud
          searchPaths: config-data
          username: s{GIT_USER}
          password: ${GIT_PASSWORD}
  security:
    user:
      name: root
      password: s3cr3t